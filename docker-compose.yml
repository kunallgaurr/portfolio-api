version: "3.9"

services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: portfolio-server
    ports:
      - "8000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
